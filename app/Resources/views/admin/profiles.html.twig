<!doctype html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport"
          content="width=device-width, user-scalable=no, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>Document</title>
</head>
<body>
<h1>info des comptes</h1>
<a href="{{ path('register') }}">Ajouter un utilisateur</a>
{% for user in user %}
    <p>Prénom : {{ user.firstname }}</p>
    <p>Nom : {{ user.lastname }}</p>
    <p>adresse email : {{ user.email }}</p>
    <p>member_id : {{ user.memberid }}</p>
    <a href="{{ path('admin_edit', {'id': user.id}) }}">modifier l'utilisateur</a>
    <a href="{{ path('user-delete', {'id': user.id}) }}">supprimer l'utilisateur</a>
    <a href="{{ path('user-view', {'id': user.id}) }}">view user</a>
<br><br>
    <hr>
{% endfor %}

</body>
</html>